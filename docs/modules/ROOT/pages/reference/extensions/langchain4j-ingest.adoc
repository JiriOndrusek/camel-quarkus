// Do not edit directly!
// This file was generated by camel-quarkus-maven-plugin:update-extension-doc-page
[id="extensions-langchain4j-ingest"]
= LangChain4j Ingest
:linkattrs:
:cq-artifact-id: camel-quarkus-langchain4j-ingest
:cq-native-supported: true
:cq-status: Stable
:cq-status-deprecation: Stable
:cq-description: Declarative AI document ingestion: point a knowledge base at a folder via configuration; parsing, splitting, embedding and storing are handled under the hood
:cq-deprecated: false
:cq-jvm-since: 3.39.0
:cq-native-since: 3.39.0

ifeval::[{doc-show-badges} == true]
[.badges]
[.badge-key]##JVM since##[.badge-supported]##3.39.0## [.badge-key]##Native since##[.badge-supported]##3.39.0##
endif::[]

Declarative AI document ingestion: point a knowledge base at a folder via configuration; parsing, splitting, embedding and storing are handled under the hood

[id="extensions-langchain4j-ingest-maven-coordinates"]
== Maven coordinates

https://{link-quarkus-code-generator}/?extension-search=camel-quarkus-langchain4j-ingest[Create a new project with this extension on {link-quarkus-code-generator}, window="_blank"]

Or add the coordinates to your existing project:

[source,xml]
----
<dependency>
    <groupId>org.apache.camel.quarkus</groupId>
    <artifactId>camel-quarkus-langchain4j-ingest</artifactId>
</dependency>
----
ifeval::[{doc-show-user-guide-link} == true]
Check the xref:user-guide/index.adoc[User guide] for more information about writing Camel Quarkus applications.
endif::[]

[id="extensions-langchain4j-ingest-usage"]
== Usage
The extension turns document ingestion into configuration. Declare a knowledge base (an _ingestion
pipeline_) and point it at a folder:

[source,properties]
----
quarkus.camel.ai.ingest.products.source.type=file
quarkus.camel.ai.ingest.products.source.directory=/data/docs
quarkus.camel.ai.ingest.products.mode=sync
----

Every file found is parsed, split into segments, embedded with the application's `EmbeddingModel`
bean and written to its `EmbeddingStore` bean. When more than one store or model bean exists,
select them explicitly per pipeline:

[source,properties]
----
quarkus.camel.ai.ingest.products.embedding-store=products
quarkus.camel.ai.ingest.products.embedding-model=my-model
----

[id="extensions-langchain4j-ingest-usage-staying-correct-mode-sync"]
=== Staying correct: `mode=sync`

With `mode=sync` the knowledge base *mirrors* its source: an edited document replaces its
previous vectors instead of joining them, a document that shrank loses its stale segments, and an
unchanged corpus costs nothing on restart — change detection uses a cheap fingerprint first
(file size + modification time) and a content hash second, so unchanged documents are skipped
without splitting or embedding.

Sync mode records what was ingested in a small ledger table (`cq_ingest_ledger`) in the
application's datasource. In dev and test mode, Dev Services provides the database
automatically; in production configure `quarkus.datasource.*` as usual, or point the ledger at a
named datasource:

[source,properties]
----
quarkus.camel.ai.ingest.products.ledger.datasource=my-datasource
----

Segment identifiers are deterministic, so on stores whose write overwrites by id (pgvector,
qdrant, elasticsearch) replacement is a plain upsert. Stores that keep or mix old content on
same-id writes (chroma, milvus, the in-memory store) need:

[source,properties]
----
quarkus.camel.ai.ingest.products.write-strategy=remove-then-add
----

Declare the embedding model's identity so a provider-side model change can be detected — bump it
to force re-embedding:

[source,properties]
----
quarkus.camel.ai.ingest.products.embedding-model-id=openai/text-embedding-3-small@2024-01
----

`mode=append` remains available for disposable stores and demos: documents are only ever added,
a restart re-ingests the corpus, and no datasource is needed.

[id="extensions-langchain4j-ingest-usage-deletion-safely"]
=== Deletion, safely

In sync mode a document that disappears from the source disappears from the knowledge base — on
the next pass, and only when the pass is safe: a pass whose enumeration failed or that had any
processing failure deletes nothing (an undelivered document is indistinguishable from a
disappeared one), and a pass refusing to delete more than 10 % of a pipeline's documents without
explicit consent turns a mistyped directory or a failed mount into a warning instead of an
emptied knowledge base:

[source,properties]
----
quarkus.camel.ai.ingest.products.reconcile.bulk-delete-threshold=0.1
quarkus.camel.ai.ingest.products.reconcile.allow-bulk-delete=false
----

[id="extensions-langchain4j-ingest-usage-correcting-the-knowledge-base-from-code"]
=== Correcting the knowledge base from code

[source,java]
----
@Inject IngestOperations ingest;

ingest.delete("products", "manuals/withdrawn.pdf");   // sticks: survives every future pass
ingest.upsert("products", "manuals/spec.pdf", correctedText); // pins: the source stops updating it
ingest.unsuppress("products", "manuals/withdrawn.pdf"); // hands it back to the source
ingest.unpin("products", "manuals/spec.pdf");
----

`delete` records a tombstone, so the document stays deleted even while its source file still
exists — a legal hold that reverts on the next poll would be worse than none. `upsert` over a
source-owned document pins it for the same reason: a correction must not silently revert.
Documents written through `IngestOperations` or the ingress are `api`-origin and are never
deleted for not appearing in a source listing.

[id="extensions-langchain4j-ingest-usage-sources"]
=== Sources

[cols="1,2,2,2"]
|===
|Type |Options |Document id / fingerprint |Deletion signal

|`file`
|`directory`, `recursive`, `include`
|relative path / size + mtime
|file disappears

|`http`
|`url`, `poll-interval`
|the URL / `ETag` or `Last-Modified` (HEAD)
|`404` / `410`

|`s3`
|`bucket`, `region`, `prefix`, `access-key`, `secret-key`, `endpoint-override`
|object key / ETag (from the listing, before any download)
|object disappears

|`kafka`
|`topic`, `brokers`, `from-beginning`
|record key / — (streams re-deliver; the content hash deduplicates)
|null-payload record (compacted-topic tombstone)

|`endpoint`
|`uri` (build-time fixed)
|`CamelAiIngestDocumentId` header / optional `CamelAiIngestFingerprint` header
|— (no enumeration)
|===

The `s3` and `kafka` sources need their Camel connector extension on the classpath — a missing
one stops the build with the exact `quarkus:add-extension` command.

Credentials and endpoint options are optional here on purpose: everything except the source's
identity (`bucket`, `topic`, `url`, `directory`) may instead be configured once at the Camel
component level, the standard way — for example `camel.component.aws2-s3.access-key` and
`camel.component.kafka.brokers`. Only options that are set in `quarkus.camel.ai.ingest.*`
override the component-level configuration.

For `kafka` and `endpoint` sources — where a document is a Camel exchange — processing failures
follow `on-failure`: `skip` (default: log, count, continue), `fail` (propagate to the
consumer's error handling) or `dead-letter` (Camel's Dead Letter Channel routes the failed
exchange to `dead-letter-uri`, for example a DLQ topic). Scan sources dead-letter through the
ledger instead: a poison document is retried only when its content changes. Deletion always runs behind
the pass interlock: a failed enumeration or any processing failure deletes nothing, and a pass
refuses to delete more than `reconcile.bulk-delete-threshold` (default 10 %) of a pipeline's
documents without `reconcile.allow-bulk-delete=true`.

[id="extensions-langchain4j-ingest-usage-the-type-safe-builder"]
=== The type-safe builder

Every property has a builder twin (enforced by a parity test). Prefer Java with IDE
completion? The same model:

[source,java]
----
@Ingest("products")
IngestPipeline productDocs() {
    return IngestPipeline.from(Source.s3("product-docs").region("eu-west-1"))
            .sync()
            .embeddingStore("products")
            .splitter("recursive", 500, 50);
}
----

`Source.endpoint(EndpointConsumerBuilder)` opens the full Camel Endpoint DSL — one static
import buys typed autocompletion over a connector's complete option set. Sharp edge: the DSL
factories all ship in one artifact, so that form compiles even when the connector extension is
absent; the missing component is reported at startup.

[id="extensions-langchain4j-ingest-usage-tenant-isolation-and-citations"]
=== Tenant isolation and citations

Every segment carries `cq_pipeline`, `cq_document_id` and (when the `CamelAiIngestTenant`
header was set at ingestion) `cq_tenant` metadata. Retrieved content therefore always knows
which document it came from — and providing a `RagRetrievalFilterSupplier` CDI bean turns the
tenant key into an actual access control, consulted on every retrieval:

[source,java]
----
@Singleton
public class TenantFilter implements RagRetrievalFilterSupplier {
    @Override
    public Filter filter(String augmentorName) {
        return metadataKey("cq_tenant").isEqualTo(currentTenant());
    }
}
----

[id="extensions-langchain4j-ingest-usage-adopting-an-existing-store"]
=== Adopting an existing store

The store cannot be enumerated, so adoption is a declaration, not a detection:
`adopt=assume-empty` (default — the assertion is logged loudly), `adopt=wipe`
(`removeAll()` then full ingest; refused when several pipelines share the store) or
`adopt=coexist` (foreign vectors are never replaced or removed).

[id="extensions-langchain4j-ingest-usage-store-support-measured"]
=== Store support, measured

Deterministic segment ids are the replacement primitive. Same-id write semantics differ per
store and are verified by executable tests, not assumed (langchain4j stores at 1.17.2-beta27):

[cols="1,1,1"]
|===
|Store |`write-strategy` |sync support

|pgvector, qdrant, elasticsearch footnote:[The langchain4j elasticsearch client requires an
Elasticsearch 9.x server.]
|`upsert` (same-id write overwrites)
|full

|chroma, milvus, in-memory
|`remove-then-add` (same-id write keeps or mixes old content)
|full

|cassandra, astra-db
|—
|`append` only (no removal support)
|===

[id="extensions-langchain4j-ingest-usage-clustering"]
=== Clustering

Concurrent passes on multiple replicas converge (deterministic ids) but embed the same corpus
repeatedly. With a `CamelClusterService` present (camel-quarkus-file-cluster-service,
camel-quarkus-kubernetes-cluster-service, ...), scan passes automatically run on the leader
only; `leader-only=true` without one fails at startup. Kafka streams are consumer-group
balanced by Kafka itself.

[id="extensions-langchain4j-ingest-usage-pushing-documents-from-code-or-routes"]
=== Pushing documents from code or routes

Documents can be pushed through the `direct:ingest-<pipeline>` endpoint. The
`CamelAiIngestDocumentId` header is required — replacement and (future) deletion are built on
stable document ids; the optional `CamelAiIngestFingerprint` header enables cheap change
detection for pushed documents:

[source,java]
----
producerTemplate.requestBodyAndHeader("direct:ingest-products", text,
        IngestHeaders.DOCUMENT_ID, "manuals/warranty.pdf", IngestResult.class);
----

In dev mode, the Dev UI card *Ingestion Pipelines* shows every pipeline's counters and, for
sync pipelines, the ledger's per-document state — the answer to "why doesn't my assistant know
about document X" without any vector-store spelunking.


[id="extensions-langchain4j-ingest-quarkus-langchain4j-bom"]
== LangChain4j usage

=== Dependency management

In order to ensure alignment across all Quarkus and LangChain4j related dependencies, it is recommended to import the LangChain4j BOM as below:
[source,xml,subs=attributes+]
----
<dependencyManagement>
  <dependencies>
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j-bom</artifactId>
      <version>{langchain4j-version}</version>
      <type>pom</type>
      <scope>import</scope>
    </dependency>
  </dependencies>
  ...
</dependencyManagement>
----

Note that the import order is paramount when using maven `dependencyManagement`.
As such, one might need to import the `langchain4j-bom` before other related Camel and Quarkus BOMs.

=== Quarkus LangChain4j support

WARNING: At present, this extension is neither tested with nor intended to be used in conjunction with any Quarkus LangChain4j extensions.
Consequently, both JVM and native modes may exhibit unexpected behaviour or fail to function correctly in such configurations.

[id="extensions-langchain4j-ingest-additional-camel-quarkus-configuration"]
== Additional Camel Quarkus configuration

[width="100%",cols="80,5,15",options="header"]
|===
| Configuration property | Type | Default


a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-source-type]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-type[quarkus.camel.ai.ingest."pipeline-name".source.type]`

Source type: {@code file}, {@code http}, or {@code endpoint} (any Camel consumer
URI — the escape hatch for genuine integration problems). More curated types
(s3, kafka) arrive in later releases.
| `string`
| required icon:exclamation-circle[title=Configuration property is required]

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-source-uri]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-uri[quarkus.camel.ai.ingest."pipeline-name".source.uri]`

The Camel consumer URI feeding this pipeline ({@code endpoint} source). Fixed at
build time on purpose: a runtime-overridable consumer URI would be arbitrary
component invocation. Each message needs the {@code CamelAiIngestDocumentId}
header; {@code CamelAiIngestFingerprint} is optional. No enumeration exists, so
deletion-by-disappearance does not apply to this source.
| `string`
| 

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-mode]]`link:#quarkus-camel-ai-ingest-pipeline-name-mode[quarkus.camel.ai.ingest."pipeline-name".mode]`

Ingestion mode. {@code sync}: the knowledge base mirrors the source — an edited
document replaces its previous vectors, an unchanged corpus costs nothing on restart.
Requires a datasource for the sync ledger. {@code append}: documents are only ever
added; a restart re-ingests the corpus; no ledger needed.
| `string`
| `append`

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-write-strategy]]`link:#quarkus-camel-ai-ingest-pipeline-name-write-strategy[quarkus.camel.ai.ingest."pipeline-name".write-strategy]`

How the store behaves when the same segment ids are written again ({@code sync} mode):
{@code upsert} for stores whose write overwrites (pgvector, qdrant, elasticsearch),
{@code remove-then-add} for stores that keep or mix old content on same-id writes
(chroma, milvus, in-memory).
| `string`
| `upsert`

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-embedding-model-id]]`link:#quarkus-camel-ai-ingest-pipeline-name-embedding-model-id[quarkus.camel.ai.ingest."pipeline-name".embedding-model-id]`

User-declared identity of the embedding model, for example
{@code openai/text-embedding-3-small@2024-01}. Folded into change detection: bump it
when the provider changes the model behind an unchanged name, so the corpus re-embeds
instead of silently mixing two embedding spaces.
| `string`
| 

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-adopt]]`link:#quarkus-camel-ai-ingest-pipeline-name-adopt[quarkus.camel.ai.ingest."pipeline-name".adopt]`

What to assume about pre-existing store content. {@code assume-empty} (default): the
user asserts the store holds nothing foreign — the assertion is logged loudly.
{@code wipe}: {@code removeAll()} at startup, then full ingest — the only mode that
can guarantee its postcondition, refused when several pipelines share the store.
{@code coexist}: foreign vectors are never replaced or removed (read-only legacy
corpora). The store cannot be enumerated, so adoption is a declaration, not a
detection.
| `string`
| `assume-empty`

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-splitter]]`link:#quarkus-camel-ai-ingest-pipeline-name-splitter[quarkus.camel.ai.ingest."pipeline-name".splitter]`

How documents are split into segments before embedding: {@code recursive} or
{@code none}.
| `string`
| `recursive`

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-max-segment-size]]`link:#quarkus-camel-ai-ingest-pipeline-name-max-segment-size[quarkus.camel.ai.ingest."pipeline-name".max-segment-size]`

Maximum segment size in characters.
| `int`
| `500`

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-max-overlap-size]]`link:#quarkus-camel-ai-ingest-pipeline-name-max-overlap-size[quarkus.camel.ai.ingest."pipeline-name".max-overlap-size]`

Maximum overlap between adjacent segments in characters.
| `int`
| `50`

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-embedding-store]]`link:#quarkus-camel-ai-ingest-pipeline-name-embedding-store[quarkus.camel.ai.ingest."pipeline-name".embedding-store]`

Name of the {@code EmbeddingStore} bean this pipeline writes to. May be omitted when
exactly one store bean exists in the application.
| `string`
| 

a|icon:lock[title=Fixed at build time] [[quarkus-camel-ai-ingest-pipeline-name-embedding-model]]`link:#quarkus-camel-ai-ingest-pipeline-name-embedding-model[quarkus.camel.ai.ingest."pipeline-name".embedding-model]`

Name of the {@code EmbeddingModel} bean used to embed segments. May be omitted when
exactly one model bean exists in the application.
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-enabled]]`link:#quarkus-camel-ai-ingest-pipeline-name-enabled[quarkus.camel.ai.ingest."pipeline-name".enabled]`

Whether this pipeline starts. Useful to switch ingestion off in dev mode.
| `boolean`
| `true`

a| [[quarkus-camel-ai-ingest-pipeline-name-source-directory]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-directory[quarkus.camel.ai.ingest."pipeline-name".source.directory]`

The directory to ingest documents from ({@code file} source).
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-recursive]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-recursive[quarkus.camel.ai.ingest."pipeline-name".source.recursive]`

Whether subdirectories are ingested too ({@code file} source).
| `boolean`
| `true`

a| [[quarkus-camel-ai-ingest-pipeline-name-source-include]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-include[quarkus.camel.ai.ingest."pipeline-name".source.include]`

Ant-style include pattern, for example {@code **&#47;*.txt} ({@code file} source).
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-url]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-url[quarkus.camel.ai.ingest."pipeline-name".source.url]`

The document URL ({@code http} source). The URL is the document id; change
detection uses {@code ETag} / {@code Last-Modified} via a cheap HEAD request.
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-bucket]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-bucket[quarkus.camel.ai.ingest."pipeline-name".source.bucket]`

The bucket to ingest from ({@code s3} source). The object key is the document id;
the ETag is the change fingerprint.
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-region]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-region[quarkus.camel.ai.ingest."pipeline-name".source.region]`

AWS region ({@code s3} source).
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-prefix]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-prefix[quarkus.camel.ai.ingest."pipeline-name".source.prefix]`

Only objects whose key starts with this prefix are ingested ({@code s3} source).
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-access-key]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-access-key[quarkus.camel.ai.ingest."pipeline-name".source.access-key]`

Access key ({@code s3} source).
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-secret-key]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-secret-key[quarkus.camel.ai.ingest."pipeline-name".source.secret-key]`

Secret key ({@code s3} source).
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-endpoint-override]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-endpoint-override[quarkus.camel.ai.ingest."pipeline-name".source.endpoint-override]`

Endpoint override for S3-compatible stores such as MinIO ({@code s3} source).
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-topic]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-topic[quarkus.camel.ai.ingest."pipeline-name".source.topic]`

The topic to consume ({@code kafka} source). The record key is the document id; a
record with a null payload (a compacted-topic tombstone) deletes the document.
Records without a key are skipped with a warning — replacement needs a stable id.
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-brokers]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-brokers[quarkus.camel.ai.ingest."pipeline-name".source.brokers]`

Broker list ({@code kafka} source). When not set, the component-level
{@code camel.component.kafka.brokers} configuration applies.
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-source-from-beginning]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-from-beginning[quarkus.camel.ai.ingest."pipeline-name".source.from-beginning]`

Whether the consumer starts from the beginning of the topic on first start
({@code kafka} source).
| `boolean`
| `true`

a| [[quarkus-camel-ai-ingest-pipeline-name-source-poll-interval]]`link:#quarkus-camel-ai-ingest-pipeline-name-source-poll-interval[quarkus.camel.ai.ingest."pipeline-name".source.poll-interval]`

Interval between synchronisation passes in milliseconds ({@code sync} mode).
| `long`
| `5000`

a| [[quarkus-camel-ai-ingest-pipeline-name-ledger-datasource]]`link:#quarkus-camel-ai-ingest-pipeline-name-ledger-datasource[quarkus.camel.ai.ingest."pipeline-name".ledger.datasource]`

Name of the datasource backing the sync ledger. When not set, the application's
default datasource is used. Dev Services provides one automatically in dev and
test mode when a JDBC driver is present and no datasource is configured.
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-reconcile-bulk-delete-threshold]]`link:#quarkus-camel-ai-ingest-pipeline-name-reconcile-bulk-delete-threshold[quarkus.camel.ai.ingest."pipeline-name".reconcile.bulk-delete-threshold]`

A pass refusing to delete more than this fraction of the pipeline's source
documents without {@code allow-bulk-delete=true}: a mistyped directory or a
failed mount must be a refusal, not an emptied knowledge base.
| `double`
| `0.1`

a| [[quarkus-camel-ai-ingest-pipeline-name-reconcile-allow-bulk-delete]]`link:#quarkus-camel-ai-ingest-pipeline-name-reconcile-allow-bulk-delete[quarkus.camel.ai.ingest."pipeline-name".reconcile.allow-bulk-delete]`

Explicit consent to bulk deletion (for intended corpus restructurings).
| `boolean`
| `false`

a| [[quarkus-camel-ai-ingest-pipeline-name-readiness-enabled]]`link:#quarkus-camel-ai-ingest-pipeline-name-readiness-enabled[quarkus.camel.ai.ingest."pipeline-name".readiness.enabled]`

Whether this pipeline gates application readiness until its first successful
synchronisation pass ({@code sync} mode). Disable for side-feature knowledge
bases that must not take the whole application out of rotation.
| `boolean`
| `true`

a| [[quarkus-camel-ai-ingest-pipeline-name-on-failure]]`link:#quarkus-camel-ai-ingest-pipeline-name-on-failure[quarkus.camel.ai.ingest."pipeline-name".on-failure]`

What happens when a pushed document fails to process ({@code kafka} and
{@code endpoint} sources, where a document is a Camel exchange): {@code skip} (log,
count, continue), {@code fail} (propagate to the consumer's error handling), or
{@code dead-letter} (route the failed exchange to {@code dead-letter-uri} via Camel's
Dead Letter Channel). Scan sources dead-letter through the ledger instead — a
document there is not an exchange.
| `string`
| `skip`

a| [[quarkus-camel-ai-ingest-pipeline-name-dead-letter-uri]]`link:#quarkus-camel-ai-ingest-pipeline-name-dead-letter-uri[quarkus.camel.ai.ingest."pipeline-name".dead-letter-uri]`

The Camel endpoint receiving failed exchanges when {@code on-failure=dead-letter},
for example a DLQ topic.
| `string`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-leader-only]]`link:#quarkus-camel-ai-ingest-pipeline-name-leader-only[quarkus.camel.ai.ingest."pipeline-name".leader-only]`

Whether synchronisation passes run on the cluster leader only — a cost optimisation:
concurrent passes on multiple replicas converge (deterministic segment ids) but embed
the same documents repeatedly. Default: enabled automatically when a
{@code CamelClusterService} is present (for example camel-quarkus-file-cluster-service
or camel-quarkus-kubernetes-cluster-service). {@code true} without a cluster service
fails at startup. Applies to scan-based sources; streams are consumer-group balanced
by Kafka itself.
| `boolean`
| 

a| [[quarkus-camel-ai-ingest-pipeline-name-embedding-batch-size]]`link:#quarkus-camel-ai-ingest-pipeline-name-embedding-batch-size[quarkus.camel.ai.ingest."pipeline-name".embedding.batch-size]`

Maximum number of segments embedded per model call.
| `int`
| `32`

a| [[quarkus-camel-ai-ingest-pipeline-name-embedding-requests-per-minute]]`link:#quarkus-camel-ai-ingest-pipeline-name-embedding-requests-per-minute[quarkus.camel.ai.ingest."pipeline-name".embedding.requests-per-minute]`

Upper bound on embedding calls per minute. Unset means unthrottled.
| `int`
| 
|===

[.configuration-legend]
{doc-link-icon-lock}[title=Fixed at build time] Configuration property fixed at build time. All other configuration properties are overridable at runtime.

