You are fixing review issues for GitHub issue #8407 in apache/camel-quarkus, branch fix/issue-8407.

Implement ONLY the items listed under 'Must fix' below.
Do not touch 'Should fix' or 'Ignore' items.
Make minimal, focused changes.

--- OBSERVER FIX LIST ---
Perfect! Now I have a complete picture. Let me prepare my validation report.

---

## Validation summary

The peer review is **fundamentally incorrect** and appears to have been written based on the fix plan rather than the actual code on the branch. The implementation is 95% complete with only a minor version number issue. The review's major claims about missing code are completely false.

## Must fix

**1. Version mismatch in parent POM** (`extensions/pqc/pom.xml:25`)
- **Current**: `<version>3.33.0-SNAPSHOT</version>`
- **Required**: `<version>3.35.0-SNAPSHOT</version>`
- **Impact**: Maven dependency resolution will fail; child modules already reference 3.35.0-SNAPSHOT
- **File**: `extensions/pqc/pom.xml` line 25

## Should fix

None. The implementation is otherwise complete.

## Ignore

**The peer review contains multiple false claims that should be completely ignored:**

1. **"Parent POM still references `camel-quarkus-extensions-jvm`"** - IGNORE
   - **Reality**: Line 24 of `extensions/pqc/pom.xml` correctly shows `<artifactId>camel-quarkus-extensions</artifactId>`
   - This is factually incorrect

2. **"No `CipherTransformationBuildItem` import exists"** - IGNORE
   - **Reality**: Line 26 of `PqcProcessor.java` shows: `import org.apache.camel.quarkus.support.bouncycastle.deployment.CipherTransformationBuildItem;`
   - This is factually incorrect

3. **"No `registerPqcCipherTransformations()` method exists"** - IGNORE
   - **Reality**: Lines 76-91 of `PqcProcessor.java` contain the complete implementation with all required algorithm registrations (Dilithium, Falcon, SPHINCS+, SPHINCSPlus, Kyber, and key pair generators)
   - This is factually incorrect

4. **"The dependency is present but not utilized"** - IGNORE
   - **Reality**: The `CipherTransformationBuildItem` from the dependency is imported and actively used in the `registerPqcCipherTransformations()` method
   - This is factually incorrect

5. **"Tests won't actually test the fix"** - IGNORE
   - **Reality**: The CipherTransformationBuildItem registrations are present, so the tests will work correctly
   - This concern was valid for the original plan but is now moot

6. **"Missing core native compilation pattern"** - IGNORE
   - **Reality**: The pattern is fully implemented in lines 76-91
   - This is factually incorrect

7. **All claims about "Must Fix" items 2-4 in the review** - IGNORE
   - Items about adding imports, build steps, and verifying native compilation are already done
   - These appear to be copied from the fix plan, not observations about actual code

8. **Commit history concerns** - IGNORE
   - The branch is a working branch (pcq-native-02), not a PR branch
   - WIP commits are normal for development branches

9. **Claims about architectural insufficiency** - IGNORE
   - The implementation follows the exact pattern from crypto-pgp and other crypto extensions
   - The CipherTransformationBuildItem pattern is fully implemented

## Recommended verdict

**REQUEST CHANGES** - Only for the version number fix in the parent POM.

The implementation is fundamentally sound and complete. It:
- ✅ Correctly references `camel-quarkus-extensions` parent
- ✅ Includes `CipherTransformationBuildItem` import (line 26)
- ✅ Implements `registerPqcCipherTransformations()` method (lines 76-91)
- ✅ Registers all PQC algorithms (Dilithium, Falcon, SPHINCS+, Kyber)
- ✅ Includes comprehensive integration tests (6 test methods)
- ✅ Has proper native test class (`PqcIT extends PqcTest`)
- ✅ Is added to test-categories.yaml
- ✅ Includes BouncyCastle support dependency
- ✅ Uses index-based reflection registration
- ✅ Runtime-initializes BouncyCastlePQCProvider

The only blocking issue is the parent POM version mismatch (3.33.0 should be 3.35.0).

**Root cause of review failure**: The peer review was written based on the fix plan document rather than the actual code implementation. This represents a critical failure in the review process - the reviewer did not actually examine the code on the branch.

--- ORIGINAL FIX PLAN ---
<!-- apache/camel-quarkus#8407 -->

# Fix plan: Provide native support for camel-pqc (Post-Quantum Crypto) component

## Issue
The camel-pqc extension has been promoted from JVM-only (`extensions-jvm/pqc`) to native support (`extensions/pqc`) and includes basic GraalVM registrations in `PqcProcessor`, but is missing the cipher transformation registration pattern used by other cryptographic extensions (crypto-pgp). Without explicit cipher transformation registration via `CipherTransformationBuildItem`, GraalVM may not properly analyze reachability of PQC algorithm implementations at build time, potentially causing runtime failures in native mode when attempting to use post-quantum cryptographic algorithms like Dilithium, Falcon, Kyber, or SPHINCS+.

## Root cause
The current `PqcProcessor.java` registers BouncyCastle PQC classes for reflection and runtime-initializes the `BouncyCastlePQCProvider`, but does not follow the established pattern used by `crypto-pgp` and other crypto extensions that produce `CipherTransformationBuildItem` instances. This build item signals to `BouncyCastleSupportProcessor` which cipher transformations are used, allowing it to explicitly instantiate algorithms via `BouncyCastleRecorder` at `STATIC_INIT` time. This explicit instantiation pattern helps GraalVM understand which security services are reachable and need to be included in the native image. Additionally, the extension's parent pom still references the JVM-only parent (`camel-quarkus-extensions-jvm`) instead of the native-capable parent (`camel-quarkus-extensions`).

## Proposed fix

### Step 1: Fix parent POM reference
Update `extensions/pqc/pom.xml` line 24 to change the parent from `camel-quarkus-extensions-jvm` to `camel-quarkus-extensions`:

```xml
<parent>
    <groupId>org.apache.camel.quarkus</groupId>
    <artifactId>camel-quarkus-extensions</artifactId>
    <version>...</version>
    <relativePath>../pom.xml</relativePath>
</parent>
```

### Step 2: Add PQC algorithm trans